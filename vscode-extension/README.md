# Logex VS Code Extension

Turn a Claude Code session into a blog-style article without leaving your editor. The extension registers the command **Logex: Write Article from Session**, which opens an integrated terminal and runs `logex write` against the active session so you can publish a polished write-up with a single palette action.
